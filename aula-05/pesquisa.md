- O que é um array em programação?

**R: É uma estrutra de dados, que permite armazenar dados em uma única variável.**
- Por que é melhor usar um array do que criar várias variáveis separadas, como aluno1, aluno2
e aluno3?

**R:Porque economiza dados e fica mais fácil de mexer, ao invés de varios (console.log).
- O que é índice em um array?

**R: É uma posição de cada elemento dentro do array.**
- Por que o primeiro elemento de um array fica na posição 0?

**R: Poruqe é o ponto de início**
- Para que serve o .length?

**R: Sere para informar quantos elementos existem em uma array.**
- Como acessamos o primeiro elemento de um array chamado alunos?

**R: Usamos o índica 0.**
- Como podemos alterar um valor dentro de um array?

**R: Acessarndo o índice desejado e atribuir um novo valor.**
- Qual a relação entre arrays e laços de repetição?

**R:Os laços de repetição permitem percorrer todos os elementos de um array automaticamente.**
- Qual a diferença entre percorrer um array com for e apenas mostrar um item específico?

**R: no item especifíco so muda um item, no for  exibe todos os elementos.**
- O que é um acumulador? Dê um exemplo de uso com notas ou valores.

**R:Um acumulador é uma variável usada para somar ou acumular valores durante a execução do programa.**
**let notas = [8, 7, 10];**
**let soma = 0;**

**for (let i = 0; i < notas.length; i++) {**
    **soma += notas[i];**
**}**

**console.log(soma);**

**R: 25**

- Para que servem operadores aritméticos como +, -, * e / dentro de um programa?

**R:para realizar calculos amtematicos.**
- Para que servem operadores lógicos como && e ||?

**R: &&= ande/e todas as condições devem ser verdadeiras. ||=ou pelo menos uma condição deve ser verdadeira.**
- Qual a diferença entre if/else e switch?

**R:if/else = se/senão. switch = usado quando queremos comparar uma variável com vários valores específicos.**
- O que os métodos push e pop fazem em um array?

**R: push= adiciona um elemento ao final de uma array. pop= remove o ultimo elemento do array.**
- O que é uma matriz e como ela se relaciona com arrays?

**R:Uma matriz é um array que contém outros arrays, formando linhas e colunas.**